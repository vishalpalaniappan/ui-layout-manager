import { useEffect, useState } from "react";
import PropTypes from 'prop-types';

import "./Column.scss";

/**
 * Renders a column.
 * 
 */
export const Column = ({}) => {
    return (
        <></>
    );
}

Column.propTypes = {
}