import TRANSFORMATION_TYPES from "./TRANSFORMATION_TYPES";
/**
 * This class generates transformations based on the
 * childs layout configuration. For example, when the
 * handle bar is moved below a threshold, the container
 * is automatically collapsed and it is exapnded when the handler
 * bar is above a threshold.
 */
export class ChildRuleEnforcer {
    /**
     * Initialize the child rule enforcer.
     */
    constructor () {
    }
}